[project]
name = "lordabbett"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dateutil>=2.9.0.post0",
    "scipy>=1.11.0",
]

[dependency-groups]
dev = [
    "pytest>=6.1.2",
]
